<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.excercise_app_quan_li_sinh_vien">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pay"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pay"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.HomeScreenActivity"
            android:theme="@style/Navigation"></activity>
        <activity android:name=".LoginActivity.MainLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
